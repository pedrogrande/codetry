<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Codetry" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Codetry" %>">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
    <link href='http://fonts.googleapis.com/css?family=Special Elite' rel='stylesheet' type='text/css'>
    <script>
    (funtion(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]||function(){(i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),m=s.getElementsByTagName(o) [0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)})(window,document,'script','//www.google-analytics.com/analytics.js','ga');

    ga('create', 'UA-41093816-1', 'codetry.org');
    ga('send', 'pageview');
    </script>
  </head>
  <body class="<%= controller_name %> <%= action_name %>" >

    
  

    <%= render 'layouts/navigation' %>
    
      <div class="container main">
        
          <%= render 'layouts/messages' %>
          <%= yield %>
        </div>
     
  </div>
  
    <footer>
      <div class="container">
        <div class="row">
          <div class="span12">
            <%= render 'layouts/footer' %>
          </div>
        </div>
      </div>
    </div>

    </footer>  
    <%= render 'layouts/facebook_script' %>
  </body>
</html>
