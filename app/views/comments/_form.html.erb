<%= simple_form_for(@comment) do |f| %>
  <%= f.error_notification %>

<br>
<br>

  <h4>Comments</h4>
 
  <div class="form-inputs">
    <%= f.input :content => false, :input_html => { :class => "span6", :rows => 6} %> 
    <!--<%= f.association :user %>-->
  </div>

  <div class="form-actions">
    <%= f.button :submit, :class => 'btn btn-success' %>
  </div>
<% end %>
